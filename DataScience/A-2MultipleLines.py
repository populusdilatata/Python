# The code segment with statement contained within the [], {}, or ()
# brackets does not need to use the line continuation character, 
# as shown in example
days = ['Monday', 'Tuesday', 'Wednesday',
'Thursday','Friday']
print(days)